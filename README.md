# N8N-HW1.2

یک ماشین حساب کنسولی ساده برای جمع و تفریق.

## اجرا

برای اجرای برنامهٔ کنسولی با .NET 8:

```bash
dotnet run
```

در زمان اجرا یکی از عملیات زیر را وارد کنید:

- `جمع` یا `add`
- `تفریق` یا `subtract`
- `خروج` یا `exit`

سپس دو عدد را وارد کنید تا نتیجه نمایش داده شود.
